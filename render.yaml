services:
  - type: web
    name: diem-danh-ai
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app

build:
  nixpacks:
    languages:
      python:
        version: "3.10"
